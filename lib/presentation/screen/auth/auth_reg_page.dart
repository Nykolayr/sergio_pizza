import 'package:flutter/material.dart';

class AuthRegPage extends StatelessWidget {
  const AuthRegPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Text('AuthRegPage'));
  }
}
